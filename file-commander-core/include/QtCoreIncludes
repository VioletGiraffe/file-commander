#pragma once

#ifdef _WIN32
#pragma warning(push, 0) // set W0
#endif

#include <QtGlobal>
#include <QDebug>

#ifdef _WIN32
#include <QtWin>
#endif

#ifdef _WIN32
#pragma warning(pop) // set W0
#endif
